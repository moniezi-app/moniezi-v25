MONIEZI v25.4 package
- Fix Tax Summary PDF export crash: pdf-lib does not have drawRoundedRectangle
  - Implemented custom drawRoundedRectangle using drawSvgPath with cubic bezier arcs
  - Replaced all 9 call sites across cards, panels, progress bars, and section shells
- Fix secondary crash risk: non-ASCII Unicode characters in embedded font subsets
  - Added sanitizeForPdf() to strip locale-dependent chars (NBSP, en-dash, em-dash, middle dot)
  - Pinned formatCurrency/formatNumber/formatPercent to en-US locale for deterministic output
  - Sanitize all dynamic string inputs (business name, category names, attention items) at PDF entry
- Slim source-only package
